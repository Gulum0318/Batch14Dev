// Vehicle make, model and color.

string make = 'Mercedes-Benz';
system.debug(make);

string model = 'Sprinter';
system.debug(model);

string color = 'Black';
system.debug(color);

string myCar = make + ' ' + model + ' ' + color;
system.debug(myCar);